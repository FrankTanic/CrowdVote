
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<ul>
@if (Model != null)
{
    foreach (var category in Model)
    {
            <li><a href=@Url.Action("Details", "Categories", new { id = @category.ID })>@category.Title</a></li>
    }
}
else
{
    <li>There are no categories</li>
}
</ul>